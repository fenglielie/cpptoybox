name: Test-CI

on:
  push:
    branches:
      - 'main'

env:
  BUILD_TYPE: Release

jobs:
  run-bvt-gcc13:
    uses: ./.github/workflows/bvt-gcc13.yml
    name: run bvt with g++ 13

  run-bvt-clang15:
    uses: ./.github/workflows/bvt-clang18.yml
    name: run bvt with clang 18

  run-bvt-msvc14:
    uses: ./.github/workflows/bvt-msvc17.yml
    name: run bvt with msvc17 (vs2022)
